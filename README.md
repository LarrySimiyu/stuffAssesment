# Instructions
    Install `npm` by running
    1. Run `npm start` in root project folder
    2. Run `npm install @fortawesome/free-solid-svg-icons`
    3. Run `npm install @fortawesome/react-fontawesome`
    

    In `index.js` file, to view TaskComponent change <DatePicker /> to <App />
